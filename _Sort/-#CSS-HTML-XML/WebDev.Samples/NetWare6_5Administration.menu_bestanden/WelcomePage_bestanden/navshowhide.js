var curImageID = "tasks"; //defines initial selected button//array for all images if (document.images){   //load images for swapping   var imageArray = new Array();   imageArray["plus"] = new Image(9,9);   imageArray["minus"] = new Image(9,9);      //set images URLs   imageArray["plus"].src = "images/PLUS.GIF";   imageArray["minus"].src = "images/MINUS.GIF";}function navshowhide(divID, imageID){   var element;   element = document.getElementById(divID);      if(element.style.display == "none")   {      element.style.display = "block";      document.images[imageID].src = imageArray["minus"].src;   }   else   {      element.style.display = "none";      document.images[imageID].src = imageArray["plus"].src;   } }